
**************** assessment requirements ************************

1-- ok  Aplicação do sistema de grid corretamente,
       permitindo que o site todo se torne responsivo (2 pontos);

2-- ok  Utilização de cores, tipografias e tabelas customizadas
        (zebra, hover, header);

3-- ok       Header e footer customizado corretamente, em todo o site;

4-- ok       Menu customizado corretamente, em todo o site;

5-- Utilização de buttons, dropdown, icons e popover/toast, corretamente;

6--ok     Utilização de forms (validação), inputs e carousel corretamente;

7-- ok      Utilização de e modal e cards, corretamente;
